/*linha informa que esse será o principal pacote da aplicaçao
package main

//Importa um pacote(como em outras linguagens)
import "fmt"

func main() {
	//Hello world print
	//A primeira linha de pacotes externos(no caso fmt) tem que
	//ser dada com a primeira letra maiúscula, para indicar
	//que "o pacote é externo".
	fmt.Println("Olá Mundo")
}

//para executar o programa, basta compilar com go build xxxx.go

//para facilitar o trabalho podemos usar um go run xxx.go
//assim ele vai compilar e rodar ao mesmo o arquivo.
*/
